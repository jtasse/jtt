StylesPath = packages/docs/styles
MinAlertLevel = suggestion

[*.{html,md}]
# Inherit styles from the docs configuration
# Note: Ensure 'npm run lint:sync' has been run in packages/docs to populate styles
# You may need to adjust BasedOnStyles to match the styles present in packages/docs/styles
BasedOnStyles = Vale, Google, Microsoft